package newProject;
import apcslib.*;

public class Build extends CampusGrid {
	private DrawingTool god = new DrawingTool(new SketchPad(1000,1000));
	
	Build() {
		//	Builds campus map
	}
	
	Build(Object E6grid) {
		//	Builds the E6 Grid
	}
}
